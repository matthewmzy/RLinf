# RealSense camera configuration for real-world tasks.
# Supported fields: name, crop_region, resolution, fps, enable_depth.

camera_defaults:
  crop_region: null
  resolution: [640, 480]
  fps: 15
  enable_depth: false

overrides:
  "105322251046":
    crop_region: [0.4, 0.3, 0.85, 0.7]
    name: "global"
  "218722271009":
    name: "wrist_1"
