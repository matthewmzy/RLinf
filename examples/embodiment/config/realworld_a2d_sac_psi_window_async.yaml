defaults:
  - preset/realworld_a2d_sac_psi_async_base@_global_
  - _self_

hydra:
  run:
    dir: .
  output_subdir: null

runner:
  logger:
    experiment_name: "a2d_sac_psi_window_async"

env:
  train:
    override_cfg:
      task_name: "A2D psi-policy window async"
  eval:
    override_cfg:
      task_name: "A2D psi-policy window async eval"

rollout:
  action_execution:
    mode: window_blend
