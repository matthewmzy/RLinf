defaults:
  - preset/realworld_a2d_dagger_psi_base@_global_
  - _self_

hydra:
  run:
    dir: .
  output_subdir: null

runner:
  logger:
    experiment_name: "a2d_dagger_psi_direct"

env:
  train:
    override_cfg:
      task_name: "A2D psi-policy HG-Dagger direct"
  eval:
    override_cfg:
      task_name: "A2D psi-policy HG-Dagger direct eval"

rollout:
  action_execution:
    mode: direct
