defaults:
  - preset/realworld_a2d_dagger_psi_base@_global_
  - _self_

hydra:
  run:
    dir: .
  output_subdir: null

runner:
  logger:
    experiment_name: "a2d_dagger_psi_window"

env:
  train:
    override_cfg:
      task_name: "A2D psi-policy HG-Dagger window"
  eval:
    override_cfg:
      task_name: "A2D psi-policy HG-Dagger window eval"

rollout:
  action_execution:
    mode: window_blend
